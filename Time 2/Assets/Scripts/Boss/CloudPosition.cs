using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CloudPosition : MonoBehaviour
{
    [Header("Define se seria uma possivel posicao para o inimigo nuvem")]
    public bool isCloudPosition;
    [HideInInspector]public bool haveRained = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
